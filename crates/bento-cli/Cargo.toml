[package]
name = "bento-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow     = "1"
clap       = { version = "4", features = ["derive"] }
env_logger = "0.11"
libbento   = { path = "../libbento" }
log        = "0.4"
