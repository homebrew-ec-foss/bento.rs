[package]
name = "libbento"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0.98"
nix         = { version = "0.27", features = ["user", "mount", "fs"]}

[dev-dependencies]
tempfile = "3.0"
lazy_static = "1.4"
